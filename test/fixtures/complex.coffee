fn = ->
  if outerCondition
    if innerCondition
      if innerCondition
        if innerCondition
          if innerCondition
            if innerCondition
              if innerCondition
                if innerCondition
                  if innerCondition
                    if innerCondition
                      if innerCondition
                        if innerCondition
                          doSomething()

secondFn = ->
  if outerCondition
    if innerCondition
      if innerCondition
        if innerCondition
          if innerCondition
            if innerCondition
              if innerCondition
                if innerCondition
                  if innerCondition
                    if innerCondition
                      if innerCondition
                        if innerCondition
                          doSomething()

thirdFn = ->
  if outerCondition
    if innerCondition
      if innerCondition
        if innerCondition
          if innerCondition
            if innerCondition
              if innerCondition
                if innerCondition
                  if innerCondition
                    if innerCondition
                      if innerCondition
                        if innerCondition
                          doSomething()

fourthFn = ->
  if outerCondition
    if innerCondition
      if innerCondition
        if innerCondition
          if innerCondition
            if innerCondition
              if innerCondition
                if innerCondition
                  if innerCondition
                    if innerCondition
                      if innerCondition
                        if innerCondition
                          doSomething()
