// Generated by CoffeeScript 1.7.1
(function() {
  var churn, complexity, count, execSync, files, fs, glob, gpa, isLiterate, report, rules, tokens, tokensForFile;

  execSync = require("exec-sync");

  tokens = require("coffee-script").tokens;

  rules = require("../lib/rules");

  fs = require("fs");

  glob = require("glob");

  isLiterate = function(path) {
    return /\.litcoffee|\.coffee\.md/i.test(path);
  };

  tokensForFile = function(path) {
    var file;
    file = fs.readFileSync(path, "utf8");
    return tokens(file, {
      literate: isLiterate(path)
    });
  };

  churn = function(filePath) {
    var command, output;
    command = "git whatchanged " + filePath + " | grep 'commit' | wc -l";
    output = execSync(command);
    return parseInt(output, 10);
  };

  count = function(filePath) {
    return tokensForFile(filePath).length;
  };

  complexity = function(filePath) {
    return tokensForFile(filePath).reduce(function(sum, token) {
      var type;
      type = token[0];
      return sum + (rules[type] || 0);
    }, 0);
  };

  gpa = function(filePath) {
    var base, longFilePenalty, penalized, tokenCount;
    tokenCount = count(filePath);
    if (tokenCount === 0) {
      return 0;
    }
    if ((0 <= tokenCount && tokenCount <= 200)) {
      longFilePenalty = 0;
    } else if ((200 < tokenCount && tokenCount <= 300)) {
      longFilePenalty = 0.25;
    } else if ((300 < tokenCount && tokenCount <= 500)) {
      longFilePenalty = 0.5;
    } else if (tokenCount > 500) {
      longFilePenalty = 1;
    }
    base = tokenCount / complexity(filePath);
    penalized = (base * 4) - longFilePenalty;
    return Math.max(penalized, 0);
  };

  files = function(paths) {
    return paths.reduce(function(list, path) {
      var pattern, stats;
      stats = fs.lstatSync(path);
      if (stats.isFile()) {
        list.push(path);
      } else if (stats.isDirectory()) {
        pattern = "" + path + "/**/*\.+(coffee|coffee\.md|litcoffee)";
        list = list.concat(glob.sync(pattern));
      }
      return list;
    }, []);
  };

  report = function(filePaths, opts) {
    var scores;
    if (opts == null) {
      opts = {};
    }
    scores = files(filePaths).reduce(function(hash, file) {
      hash[file] = {
        gpa: gpa(file),
        churn: churn(file),
        complexity: complexity(file),
        tokenCount: count(file)
      };
      return hash;
    }, {});
    return JSON.stringify(scores, null, opts.indentSpace);
  };

  exports.clog = {
    report: report,
    VERSION: "0.0.10"
  };

}).call(this);
