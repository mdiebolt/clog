// Generated by CoffeeScript 1.7.1
(function() {
  var churn, execSync, files, fs, glob, read, report, rules, score, tokens;

  execSync = require("execSync");

  tokens = require("coffee-script").tokens;

  rules = require("../lib/rules").rules;

  fs = require("fs");

  glob = require("glob");

  read = function(path) {
    return fs.readFileSync(path, "utf8");
  };

  churn = function(filePath) {
    var command, output;
    command = "git whatchanged " + filePath + " | grep 'commit' | wc -l";
    output = execSync.exec(command);
    return parseInt(output.stdout, 10);
  };

  score = function(filePath) {
    var file, literate;
    literate = /\.litcoffee|\.coffee\.md/.test(filePath);
    file = read(filePath);
    return tokens(file, {
      literate: literate
    }).reduce(function(sum, token) {
      var type;
      type = token[0];
      return sum + (rules[type] || 0);
    }, 0);
  };

  files = function(paths) {
    return paths.reduce(function(list, path) {
      var pattern, stats;
      stats = fs.lstatSync(path);
      if (stats.isFile()) {
        list.push(path);
      } else if (stats.isDirectory()) {
        pattern = "" + path + "/**/*.+(coffee|coffee\.md|litcoffee)";
        list = list.concat(glob.sync(pattern));
      }
      return list;
    }, []);
  };

  report = function(filePaths) {
    return files(filePaths).reduce(function(hash, file) {
      hash[file] = {
        churn: churn(file),
        complexity: score(file)
      };
      return hash;
    }, {});
  };

  exports.clog = {
    churn: churn,
    score: score,
    report: report,
    VERSION: "0.0.9"
  };

}).call(this);
