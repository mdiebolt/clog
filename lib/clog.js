// Generated by CoffeeScript 1.6.2
(function() {
  var BLANK_LINES, anonymousId, churn, countNodes, eachExpression, eachProperty, exec, fs, getMethods, getNode, glob, methods, nodes, spawn, _ref;

  _ref = require('child_process'), spawn = _ref.spawn, exec = _ref.exec;

  nodes = require('coffee-script').nodes;

  fs = require('fs');

  glob = require('glob');

  BLANK_LINES = /^\s*$[\n\r]{1,}/gm;

  anonymousId = 0;

  getNode = function(node, totalNodes) {
    if (totalNodes == null) {
      totalNodes = 0;
    }
    node.expressions.forEach(function(n) {
      var body, _ref1;

      if ((body = (_ref1 = n.value) != null ? _ref1.body : void 0) != null) {
        getNode(body, totalNodes);
      }
      return totalNodes += 1;
    });
    return totalNodes;
  };

  eachExpression = function(node, cb) {
    return node.expressions.forEach(cb);
  };

  eachProperty = function(node, cb) {
    return node.properties.forEach(cb);
  };

  getMethods = function(node, output) {
    if (output == null) {
      output = {};
    }
    eachExpression(node, function(exp) {
      var body, end, start, _ref1, _ref2, _ref3, _ref4, _ref5, _ref6;

      if (exp.params != null) {
        start = exp.locationData.first_line;
        end = exp.locationData.last_line;
        anonymousId += 1;
        output["anonymous" + anonymousId] = end - start;
      }
      if (((_ref1 = exp.value) != null ? _ref1.params : void 0) != null) {
        start = exp.value.locationData.first_line;
        end = exp.value.locationData.last_line;
        output[exp.variable.base.value] = end - start;
      }
      if ((body = (_ref2 = exp.value) != null ? _ref2.body : void 0) != null) {
        getMethods(body, output);
      }
      if ((_ref3 = exp.value) != null ? (_ref4 = _ref3.base) != null ? _ref4.properties : void 0 : void 0) {
        return (_ref5 = exp.value) != null ? (_ref6 = _ref5.base) != null ? _ref6.properties.forEach(function(property, index) {
          var base, currentObject, _ref7;

          base = exp.value.base;
          currentObject = base.objects[index];
          if (((_ref7 = property.value) != null ? _ref7.params : void 0) != null) {
            start = property.locationData.first_line;
            end = property.locationData.last_line;
            return output[currentObject.variable.base.value] = end - start;
          }
        }) : void 0 : void 0;
      }
    });
    return output;
  };

  methods = function(filePath) {
    var file, tree;

    file = fs.readFileSync(filePath, 'utf8').replace(BLANK_LINES, '');
    tree = nodes(file);
    return getMethods(tree);
  };

  churn = function(filePath, cb) {
    return exec("git whatchanged " + filePath + " | grep 'commit' | wc -l", cb);
  };

  countNodes = function(filePath) {
    var tree;

    tree = nodes(fs.readFileSync(filePath, 'utf8'));
    return getNode(tree);
  };

  exports.clog = {
    churn: churn,
    countNodes: countNodes,
    methods: methods,
    run: function() {
      return glob('**/*.coffee', function(err, files) {
        return files.forEach(function(file) {
          return churn(file, function(err, output) {
            return console.log(output);
          });
        });
      });
    }
  };

}).call(this);
